for(int i=0;i<col;i++)
        {
            for(int j=0; j<row; j++)
            {
                // int temp = arr[row][col];
                // arr[row][col]=arr[col][row];
                // arr[col][row]=temp;
                   
                System.out.print(arr[j][i]+" ");
                
            }
            System.out.println(" ");
	   }